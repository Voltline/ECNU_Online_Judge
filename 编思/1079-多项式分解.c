#include <stdio.h>
#include <string.h>
#include <stdlib.h>

void solve()
{
    char in[100] = {0};
    gets(in);
    int len = strlen(in);
    int a = 0, b = 0, c = 0;
    if (in[0] == 'x') {
        a = 1;
    }
    for (int i = 0; i < len; i++) {
        if ()
    }

}

int main()
{


}
